# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# RAG Pipeline
# Bumped from 0.1.0/0.0.10 to satisfy langchain-experimental==0.0.55 (SemanticChunker),
# which requires langchain>=0.1.13,<0.2. Stays within the 0.1.x/0.0.x line, not the
# breaking 0.2.x jump (langchain-experimental>=0.0.60 needs langchain-core>=0.2).
langchain==0.1.20
langchain-community==0.0.38
langchain-experimental==0.0.55
chromadb==0.4.22
sentence-transformers==3.3.1

# Document Processing
pypdf==4.0.0
python-docx==1.1.0
tiktoken==0.5.2

# OCR (replaces PDF image regions with text)
# 3.2.2 is the newest paddlepaddle with Linux aarch64 wheels on PyPI; 3.3.x
# publishes those only on PaddlePaddle's own index. For the CUDA build (or a
# newer version on aarch64), install paddle separately from:
#   https://www.paddlepaddle.org.cn/packages/stable/cpu/
paddlepaddle==3.2.2
paddleocr==3.7.0
PyMuPDF==1.28.2

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.26.0

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
black==24.1.1
