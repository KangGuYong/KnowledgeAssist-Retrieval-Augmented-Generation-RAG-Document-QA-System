# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
# LangChain 1.x 마이그레이션 0단계로 먼저 올린다(docs/superpowers/specs/
# 2026-09-02-langchain-1x-migration-design.md). langchain 1.3.18은 pydantic>=2.7.4를,
# langchain-community 0.4.2는 pydantic-settings>=2.10.1을 요구하는데, 이 범프가
# LangChain 자체보다 파급면이 넓어(설정 로딩, 응답 모델, FastAPI 호환) 별도 커밋으로
# 분리했다. fastapi 0.109.0은 pydantic<3.0.0을 허용하므로 그대로 둔다.
pydantic==2.13.5
pydantic-settings==2.15.0

# RAG Pipeline
# langchain/langchain-community bumped from 0.1.0/0.0.10 to 0.1.20/0.0.38,
# still within the 0.1.x/0.0.x line. Semantic chunking is a direct port of
# the "5 Levels of Text Splitting" notebook (app/services/chunking.py) rather
# than langchain-experimental, so that package isn't a dependency here.
langchain==0.1.20
langchain-community==0.0.38
chromadb==0.4.22
sentence-transformers==3.3.1

# Document Processing
pypdf==4.0.0
python-docx==1.1.0
tiktoken==0.5.2

# OCR (replaces PDF image regions with text)
# 3.2.2 is the newest paddlepaddle with Linux aarch64 wheels on PyPI; 3.3.x
# publishes those only on PaddlePaddle's own index. For the CUDA build (or a
# newer version on aarch64), install paddle separately from:
#   https://www.paddlepaddle.org.cn/packages/stable/cpu/
paddlepaddle==3.2.2
paddleocr==3.7.0
PyMuPDF==1.28.2

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.26.0

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
black==24.1.1
